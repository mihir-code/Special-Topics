public Item deque (){
    int random = StdRandom.Intrandom(size);
    Item item = queue[random];
    if (queue[random] != queue[size - 1]){
        queue[random] = queue[size - 1];
    } 
    queue[size - 1] = null;
    size -=1: 


}